  <nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/anuncios">LedView</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="dropdown">  <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Anuncios <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><a href="/anuncios">Mantenimiento</a></li>
          </ul>
        </li>
        <li class="dropdown">  <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Reportes <span class="caret"></span></a>
          <ul class="dropdown-menu">
          </ul>
        </li>
      </ul>
      <% if cliente_signed_in? %>
        <ul class="nav navbar-nav navbar-right">
          <li class="dropdown">
            <a class="dropdown-toggle" data-toggle="dropdown">
              <i class="glyphicon glyphicon-user icon-white"></i> <%= current_cliente.nombre %> <%= current_cliente.apellido %>
              <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <!-- <li><%#= link_to raw("<i class='icon-pencil'></i> #{t(:edit_password)}"), edit_cliente_registration_path, target: "_self" %></li> -->
              <li><%= link_to raw("<i class='glyphicon glyphicon-pencil'></i> Editar"), "/clientes/#{current_cliente.id}/editar" %></li>
              <li><%= link_to raw("<i class='glyphicon glyphicon-trash'></i> #{t(:logout)}"), destroy_cliente_session_path, method: :delete, target: "_self" %></li>
              <!-- <li><a href="#"><i class="glyphicon glyphicon-ban-circle"></i> Ban</a></li> -->
            </ul>
          </li>
        </ul>
      <% end %>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>

<div class="container-fluid">
  <ng-view></ng-view>  
</div>